import { createActionType } from "../types/actions";

export const GET_PRODUCT_DETAILS = createActionType("GET_PRODUCT_DETAILS");

export const CLEAR_PRODUCT_DETAILS_STATE = createActionType(
  "CLEAR_PRODUCT_DETAILS_STATE"
);
